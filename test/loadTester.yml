config:
  target: "http://13.57.202.185:3000"
  phases:
    - duration: 60
      arrivalRate: 30
  variables:
    productIds:
      - ["soluta","delectus","eligendi", "adipisci","quod","minus","tempore","ut","eum","fugiat","perspiciatis", "Running", "Run"]
scenarios:
  - flow:
      - loop:
          - get:
              url: "/search/{{ $loopElement }}"
              # url: "/search/eligendi"
        over: productIds